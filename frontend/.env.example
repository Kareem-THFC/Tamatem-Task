# Base URL of the Flask API. Vite only exposes variables prefixed with VITE_
# to client code, which is a guard against leaking server secrets into the
# bundle -- everything here ends up readable in the browser.
VITE_API_BASE_URL=http://127.0.0.1:5000
