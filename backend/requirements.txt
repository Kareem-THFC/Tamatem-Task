Flask>=3.0,<4.0
Flask-SQLAlchemy>=3.1,<4.0
Flask-Migrate>=4.0,<5.0
Flask-JWT-Extended>=4.6,<5.0
# Adds the Access-Control-* headers the browser needs to let the frontend,
# served from a different origin, read API responses.
Flask-Cors>=4.0,<7.0
python-dotenv>=1.0,<2.0
pydantic>=2.0,<3.0
# Backs pydantic's EmailStr, which validates the address grammar on signup.
email-validator>=2.0,<3.0
pytest>=8.0,<9.0
# Production WSGI server. The Flask development server is single-threaded and
# explicitly not meant to face the internet.
gunicorn>=23.0,<24.0
# PostgreSQL driver used by the deployed instance. Local development stays on
# SQLite, which needs no driver beyond the standard library.
psycopg[binary]>=3.2,<4.0
